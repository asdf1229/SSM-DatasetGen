# query_graph_ofat.yaml
# OFAT configuration for query graph generation
# For each data graph, generate its query graph using the defaults and one-variable-at-a-time changes.

default:
  vertices_num: 10
  num_per_setting: 100

vary:
  vertices_num: [6, 8, 10, 20, 30]
